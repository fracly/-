算法.排序.插入排序法：
跟咱们抓牌的道理是一样的，抓的一张牌，插入手牌中（当前手牌是有序的）。

（1）第一种实现方式就是上述的排序逻辑，每一个元素依次插入前面已排序的数组中。
    实现方式参考 InsertionSort 的 sort1方法, 最原始的。

（2）第二种方式就是在原来的基础上，去掉额外占用的一个数组空间，通过交换的方式，来进行迭代。
    实现方式参考 SelectionSort的 sort2方法

（3）第三种方式跟第二种一样，只是一个是从前往后找，一个是从后往前找
    实现方式参考 SelectionSort的 sort3方法